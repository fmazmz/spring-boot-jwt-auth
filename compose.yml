services:
  auth-db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: auth_db
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: